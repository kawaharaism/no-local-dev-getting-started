<div class="container">

  <table class="table table-striped table-bordered">
    <tr>
      <th>name</th>
      <th>name_kana__c</th>
      <th>syubetsu__c</th>
    </tr>

    <% @contacts.each do |contact| %>
      <tr>
        <td><%= contact.name %></td>
        <td><%= contact.name_kana__c %></td>
        <td><%= contact.syubetsu__c %></td>
      </tr>
    <% end %>
  </table>

</div>
